subscription RunningSessionUpdated($sessionId: ID!) {
  runningSessionUpdated(sessionId: $sessionId) {
    id
    activeStep
    userCount
    blocks {
      id
      status
      timeLimit
      expiresAt
    }
  }
}
